create table materii (
    id int primary key,
    titlu varchar2(128),
    an int,
    semestru int,
    pagina_cursului varchar2(255),
    titular varchar2(128),
    pagina_titular varchar2(255)
);
